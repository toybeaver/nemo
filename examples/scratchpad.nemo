###================================================
this is a multiline comment
###================================================

func main()
{
	var x: int32;
	var y: int32; 

	var dumb_bool: bool;
	var should_early_quit: bool;

	x = 5;
	y = x + 3 - 2 * 2 + 7 / 2;

	dumb_bool = true;
	should_early_quit = dumb_bool && true && true;

	if should_early_quit {
		if true {
			if false {
				exit 42;
			}
			if false {
				exit 69;
			}
			exit 40;
		}
		exit 23;
	}
	# z = false;

	if dumb_bool { exit 10; }
}
